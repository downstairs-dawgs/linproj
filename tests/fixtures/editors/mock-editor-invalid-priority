#!/bin/bash
# Mock editor that sets an invalid priority (triggers validation error)
# Cross-platform sed -i (macOS vs Linux)
if [[ "$OSTYPE" == "darwin"* ]]; then
  sed -i '' 's/priority: .*/priority: nope/' "$1"
else
  sed -i 's/priority: .*/priority: nope/' "$1"
fi
exit 0
